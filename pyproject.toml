[project]
name = "dinero-cli"
version = "0.0.0"
description = "Dinero command-line interface (binary distribution)"
requires-python = ">=3.12"
license = "LicenseRef-Apache-2.0-with-Commons-Clause-1.0"
license-files = ["LICENSE", "NOTICE"]
dependencies = [
    "typer>=0.16,<1",
]

[project.scripts]
dinero = "dinero_cli.cli:main"

[build-system]
requires = ["hatchling==1.29.0"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/dinero_cli"]

[tool.ruff]
line-length = 100
target-version = "py312"

[tool.ruff.lint]
select = ["E", "F", "I", "UP"]

[tool.pytest.ini_options]
pythonpath = [".", "src"]
testpaths = ["tests"]

[tool.coverage.run]
source = ["dinero_cli", "scripts", "devtools"]
branch = true

[tool.coverage.report]
fail_under = 81
show_missing = true
exclude_also = ['if __name__ == "__main__":']

[tool.mypy]
files = ["src", "scripts", "devtools"]
check_untyped_defs = true

[dependency-groups]
build = [
    "hatchling==1.29.0",
    "pyinstaller>=6.22,<7",
]
dev = [
    "mypy>=2.3.1",
    "pre-commit>=4.6.2",
    "pytest>=9.1.1",
    "pytest-cov>=7.1.0",
    "pyyaml>=6.0.3",
    "ruff>=0.16.8",
]
